1. unique_ptr<int> p2 = p1;
A) Ок, копіюється власність • B) Помилка компіляції ✅ • D) Runtime error

2. vector забезпечує: 
A) довільний доступ ✅ • B) двонапрямні ітератори • C) лише послідовний доступ • D) асоціативний доступ

3. Віртуальний деструктор потрібен щоб: 
 A) прискорити видалення • B) коректно видаляти через базовий вказівник ✅ • C) заборонити наслідування • D) увімкнути RTTI

4. std::list має ітератори: 
A) random access • B) bidirectional ✅ • C) forward • D) input

5. erase(remove_if(v.begin(), v.end(), pred), v.end()) — це: 
A) UB • B) «ідiоматичне» видалення за умовою ✅ • C) сортування • D) shrink_to_fit

6. nlohmann::json j; j["a"]=1; — тип j: 
A) масив • B) об’єкт ✅ • C) null • D) примітив

7. try { ... } catch(...) {} перехоплює: 
A) лише std::exception • B) лише runtime_error • C) будь-що ✅ • D) нічого